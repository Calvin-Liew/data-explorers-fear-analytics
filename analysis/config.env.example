# Copy this file to analysis/config.env and add your own key before rerunning
# the AI analysis pipeline. Do not commit config.env.
OPENAI_API_KEY=your_openai_api_key_here

# Optional model settings used by the original parser.
# OPENAI_MODEL_PRIMARY=gpt-4o-mini
# OPENAI_MODEL_FALLBACK=gpt-4o

# Optional retry/rate-limit settings.
# OPENAI_MAX_RETRIES=2
# OPENAI_SLEEP_BETWEEN=0.5
